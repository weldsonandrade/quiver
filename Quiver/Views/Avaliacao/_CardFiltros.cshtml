
<div class="row">
    @* DATA FINAL *@
    <div class="col-lg-4 m-b-20">
        <small>Data Programada Inicial</small>
        <div class="input-group form-group  m-b-0" style="margin-top: -4px;">
            <span class="input-group-addon p-r-0"><i class="zmdi zmdi-calendar"></i></span>
            <div class="dtp-container fg-line">
                <input type="text" id="dataInicial" placeholder="Sem data inicial" class="form-control date-picker">
            </div>
        </div>
    </div>
    @* DATA FINAL *@
    <div class="col-lg-4 m-b-20">
        <small>Data Programada Final</small>
        <div class="input-group form-group  m-b-0" style="margin-top: -4px;">
            <span class="input-group-addon p-r-0"><i class="zmdi zmdi-calendar"></i></span>
            <div class="dtp-container fg-line">
                <input type="text" id="dataFinal" placeholder="Sem data final" class="form-control date-picker">
            </div>
        </div>
    </div>

</div>

<div class="row">

    @** EXCLUSIVIDADE USUÁRIOS *@
    <div class="col-lg-4 m-b-10" id="filtrar-usuario">
        <small>Usuários</small>
        <br />
        <select class="tag-select" multiple data-placeholder="Todos os usuários..." name="DDLFilterUsuario" id="DDLFilterUsuario"
                style="width:100%">
            @foreach (var usuario in (IEnumerable<SelectListItem>)ViewBag.Usuarios)
            {
                <option value=@usuario.Value>@usuario.Text</option>
            }
        </select>
    </div>

    @** EXCLUSIVIDADE UNIDADES *@
    <div class="col-lg-4 m-b-10" id="filtrar-unidade">
        <small>Unidades</small>
        <select class="tag-select" multiple data-placeholder="Todas as unidades..." name="DDLFilterUnidade" id="DDLFilterUnidade"
                style="width:100%">
            @foreach (var unidade in (IEnumerable<SelectListItem>)ViewBag.Unidades)
            {
                <option value=@unidade.Value>@unidade.Text</option>
            }
        </select>
    </div>

    @** EXCLUSIVIDADE GRUPOS *@
    <div class="col-lg-4 m-b-10" id="filtrar-grupo">
        <small>Grupos</small>
        <br />
        <select class="tag-select" multiple data-placeholder="Todos os grupos..." name="DDLFilterGrupo" id="DDLFilterGrupo" style="width:100%">
            @foreach (var grupo in (IEnumerable<SelectListItem>)ViewBag.Grupos)
            {
                <option value=@grupo.Value>@grupo.Text</option>
            }
        </select>
    </div>


</div>

<div class="row">
    <div class="col-md-6 p-t-10">
        <small>De acordo com a origem da inspeção</small>
        <select class="selectpicker" id="select-agendada">
            <option>Todas</option>
            <option>Apenas as agendadas previamente pelo gerente</option>
            <option>Apenas as geradas pelo usuário no mobile</option>
        </select>

    </div>

    <div class="col-md-6 p-t-10">
        <small>De acordo com itens não coformes na inspeção</small>
        <select class="selectpicker" id="select-conformidades">
            <option>Todas</option>
            <option>Apenas inspeções que possuem algum item não conforme</option>
            <option>Apenas inspeções que possuem todos os itens conformes</option>
        </select>
    </div>
</div>
